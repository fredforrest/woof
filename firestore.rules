rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARILY DISABLE ALL SECURITY - FOR DEBUGGING ONLY
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
